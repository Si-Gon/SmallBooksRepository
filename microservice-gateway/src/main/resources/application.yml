spring:
  main:
    web-application-type: reactive
  application:
    name: msvc-gateway
  config:
    import: 'optional:configserver:http://config:8888'

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: always