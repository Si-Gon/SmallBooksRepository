spring:
  application:
    name: msvc-eureka
  config:
    import: optional:configserver:http://config:8888

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: always